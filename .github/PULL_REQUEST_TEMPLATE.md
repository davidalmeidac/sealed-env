<!--
  Thanks for the PR! Before you submit, please:
  1. Read CONTRIBUTING.md if you haven't yet.
  2. Make sure all tests pass locally:
       cd node && npm test
       cd java && mvn -B verify   (regenerate vectors first if you touched them)
  3. If this PR touches crypto, the wire format, or the threat model, link
     the issue where the design was discussed.
-->

## Summary

<!-- One paragraph: what does this PR do, and why? -->

## Type of change

- [ ] Bug fix (no API change)
- [ ] New feature (additive, no existing API changes)
- [ ] Breaking API change (specify migration path below)
- [ ] Documentation only
- [ ] Refactor (no behavior change)
- [ ] CI / tooling

## Scope

Check all that apply:

- [ ] **Touches `node/`** (TypeScript implementation)
- [ ] **Touches `java/`** (sealed-env-core or spring-boot-starter)
- [ ] **Touches the wire format** (`SPEC.md` updated, `test-vectors/v1/` updated)
- [ ] **Touches crypto code** (KDF, AES-GCM, HMAC, HKDF, TOTP, unseal token)
- [ ] **Touches the threat model** (`THREAT_MODEL.md` updated)
- [ ] **Touches docs** (`/docs/`, `README.md`, landing in `/site/`)

## Cross-stack interop

<!-- Required if the PR touches the wire format, crypto, or shared spec. -->

- [ ] Updated `test-vectors/v1/` to cover the new behavior.
- [ ] Java tests pass against vectors generated by Node.
- [ ] N/A — change does not affect cross-stack format.

## Tests

- [ ] Unit tests added / updated.
- [ ] Integration tests added / updated.
- [ ] Existing tests still pass locally.
- [ ] N/A — docs / cosmetic change only.

## Security review

<!-- Required if "Touches crypto code" is checked. -->

- [ ] Constant-time comparison used for any secret comparison.
- [ ] Derived keys are wiped after use (`wipe()` in both implementations).
- [ ] No new dependencies pulled in for crypto. (If yes, justify below.)
- [ ] AAD construction unchanged, OR change documented in SPEC + threat model.
- [ ] N/A — change does not touch crypto.

## Linked issue(s)

<!-- e.g. Closes #42 -->

## Notes for the reviewer

<!-- Anything the reviewer should pay extra attention to. -->

## Migration path (only if breaking)

<!-- If this is a breaking API change, what does a user need to do to upgrade? -->
